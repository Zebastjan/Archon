# Archon Project Configuration
project:
  name: archon
  language: nim
  auto_sync: true
  sync_on_commit: true
  sync_on_branch_switch: true
  
code_intelligence:
  extract_entities: true
  build_relationships: true
  generate_embeddings: true
  index_frequency: commit
  
analysis:
  semgrep_rules: ["security", "quality"]
  audit_on_change: true
  coverage_threshold: 80

mcp:
  transport: sse
  host: 0.0.0.0
  port: 8051
  # Stdio server available for OctoFriend at: /home/zebastjan/dev/archon/python/archon-mcp-stdio
